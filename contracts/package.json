{
  "name": "farm2shelf-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Farm2Shelf platform",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "node deploy.js",
    "deploy:local": "node deploy-local.js",
    "deploy:goerli": "hardhat run deploy.js --network goerli",
    "deploy:sepolia": "hardhat run deploy.js --network sepolia",
    "test": "hardhat test",
    "verify": "hardhat verify",
    "node": "hardhat node"
  },
  "dependencies": {
    "ethers": "^6.7.1",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.17.1"
  }
}
